# P2 Codex SQLite Tasks

## 目标

把 P2 拆成后续 agent 可执行的任务。当前文档完成后，才能进入 P2 功能开发。

## 前置文档

开发前必须阅读：

- `AGENTS.md`
- `docs/archive/P2-2026-04-30-AGENTS.md`
- `docs/contracts/P2-2026-04-30-database-schema.md`
- `docs/contracts/P2-2026-04-30-ingestion-api.md`
- `docs/milestones/P2-codex-sqlite/P2-2026-04-30-design.md`
- `docs/contracts/P1-2026-04-29-report-api.md`
- `docs/research/P1-2026-04-29-codex-log-research.md`

## Backend Agent

任务：

- [x] 增加 SQLite 初始化和 migration。
- [x] 实现 `source_files` repository。
- [x] 实现 `usage_events` repository。
- [x] 实现 `--ingest` CLI。
- [x] 把 P1 JSONL 解析逻辑复用到 ingestion。
- [x] 实现从 SQLite 聚合 `/api/report`。
- [x] 保持 P1 `/api/report` contract 兼容。
- [x] dashboard 服务启动前执行一次本地 Codex ingestion，避免页面只读空库。
- [x] 默认 SQLite 存储改为按月分片目录，并保留 `--db` 单文件兼容。

验收：

- [x] 首次 ingestion 插入 usage events。
- [x] 二次 ingestion 不重复插入。
- [x] 新增 JSONL 行后只插入新增 delta。
- [x] 数据库 schema 与 contract 一致。
- [x] 真实 mac Codex sessions 可写入临时月分片库。
- [x] 无 `token_count` 的旧 Codex session 只记录 source checkpoint，不产生 usage event。

## Test Agent

任务：

- [x] 准备脱敏 JSONL fixture。
- [x] 覆盖 cumulative snapshot delta。
- [x] 覆盖重复 ingestion 去重。
- [x] 覆盖 report contract 字段。
- [x] 覆盖月分片写入和 report 聚合。
- [x] 覆盖 dashboard startup ingestion 可触达路径；沙箱端口绑定受限时记录限制。
- [ ] 覆盖重复 snapshot 跳过。
- [ ] 覆盖文件变小或替换场景。

验收：

- [x] 测试不包含 prompt/response 正文。
- [x] 测试可在无真实 Codex 日志环境运行。

## Frontend Agent

任务：

- [x] 确认 dashboard 无需字段变更。
- [ ] 如增加 ingestion 状态展示，必须先补 API contract。

验收：

- [x] 页面仍只消费 `/api/report` contract 字段。
- [x] 不在前端计算统计口径。

## Review Agent

任务：

- [x] 检查 schema 是否存敏感内容。
- [x] 检查 ingestion 是否重复计数。
- [x] 检查 checkpoint 是否可能跳过未处理行。
- [x] 检查 `/api/report` 是否兼容 P1。
- [x] 检查 mac 月分片和启动采集变更。

产出：

- `docs/reviews/P2-2026-04-30-codex-sqlite-review.md`

## Acceptance Agent

任务：

- [x] 按 P2 acceptance 模板执行。
- [x] 记录真实终端构建结果。
- [x] 记录 ingestion 输出。
- [x] 记录 smoke test 或等价端到端验证结果。
- [x] 记录 mac 沙箱验证结果和端口绑定限制。

产出：

- `docs/acceptance/P2-2026-04-30-codex-sqlite.md`

## P2 完成定义

P2 完成必须同时满足：

- SQLite schema 初始化成功。
- `--ingest` 可导入真实 Codex usage。
- 重复 ingestion 不重复计数。
- `/api/report` 从 SQLite 返回 P1 contract 结构。
- 构建通过。
- review 通过。
- acceptance 状态为通过或有明确可接受条件。
